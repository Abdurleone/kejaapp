# In-cluster Redis for rate limiting/caching (backend/config/env.js's redisUrl) -
# not used for data that must survive a full cluster loss. No storageClassName
# is set, so it uses whatever the cluster's default StorageClass is.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: jakezapp-redis
  namespace: jakezapp
spec:
  serviceName: jakezapp-redis
  replicas: 1
  selector:
    matchLabels:
      app: jakezapp
      component: redis
  template:
    metadata:
      labels:
        app: jakezapp
        component: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          volumeMounts:
            - name: redis-data
              mountPath: /data
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: redis-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: jakezapp-redis
  namespace: jakezapp
spec:
  clusterIP: None
  selector:
    app: jakezapp
    component: redis
  ports:
    - port: 6379
