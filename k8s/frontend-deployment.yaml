apiVersion: apps/v1
kind: Deployment
metadata:
  name: jakezapp-frontend
  namespace: jakezapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jakezapp
      component: frontend
  template:
    metadata:
      labels:
        app: jakezapp
        component: frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/abdurleone/jakezapp-frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jakezapp-frontend
  namespace: jakezapp
spec:
  selector:
    app: jakezapp
    component: frontend
  ports:
    - port: 80
      targetPort: 80
