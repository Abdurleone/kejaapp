# Used by docker-compose.yml only. For running backend/frontend directly
# with npm, use backend/.env instead (copy from backend/.env.example).
JWT_SECRET=replace-with-a-long-random-string
CORS_ORIGIN=http://localhost:8080
VITE_API_BASE_URL=http://localhost:5000
